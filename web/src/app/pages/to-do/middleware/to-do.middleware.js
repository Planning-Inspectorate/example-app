export function examplesMiddleware(req, res, next) {
    //TODO: An example call to a resource to get data that will be used in the whole journey, to save us from calling it in every controller

    next();
}